$kind: http-request
description: ""
url: "{{baseUrl}}/guests/:guest_id"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: guest_id
    value: "3709"
    disabled: false
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "title": "Guest Id",
            "type": "integer"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Get Guest.resources/examples
order: 1000
