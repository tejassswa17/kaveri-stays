$kind: http-request
description: ""
url: "{{baseUrl}}/guests?email=string&limit=20&offset=0"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: email
    value: string
    disabled: false
    description: ""
    $type: string
  - key: limit
    value: "20"
    disabled: false
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "title": "Limit",
            "type": "integer",
            "default": 20,
            "minimum": 1,
            "maximum": 100
          }
        }
      }
  - key: offset
    value: "0"
    disabled: false
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "title": "Offset",
            "type": "integer",
            "default": 0,
            "minimum": 0
          }
        }
      }
body:
  type: text
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/List Guests.resources/examples
order: 1000
