$kind: http-request
description: ""
url: "{{baseUrl}}/bookings/:booking_id/check-out"
method: POST
headers:
  Accept: application/json
pathVariables:
  - key: booking_id
    value: "7720"
    disabled: false
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "title": "Booking Id",
            "type": "integer"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Check Out.resources/examples
order: 1000
